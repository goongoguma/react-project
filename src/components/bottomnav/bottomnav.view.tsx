import React from 'react';
import NavItemContainer from '@components/navItem/navItem.container';
import { BottomnavWrapper } from './bottomnav.style';

const BottomnavView = () => {
  return (
    <BottomnavWrapper>
      <NavItemContainer />
    </BottomnavWrapper>
  );
};

export default BottomnavView;
